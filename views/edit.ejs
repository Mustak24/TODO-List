<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Note : <%= note.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .hover01 {
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        z-index: 1;
        transition: all 0.4s;
        overflow: hidden;
        border: 3px solid black;

        &:hover {
          color: white;
        }

        &::after {
          content: "";
          position: absolute;
          width: 100%;
          height: 100%;
          transform: translateY(100%);
          border-radius: 50%;
          background-color: black;
          z-index: -1;
          transition: all 0.3s;
        }

        &:hover::after {
          border-radius: 0%;
          transform: translateY(0%);
        }
      }
    </style>
  </head>
  <body class="bg-green-300 px-4 overflow-x-hidden">
    <main class="flex flex-col items-center">
      <form
        action="/updateNote/<%= note.name %>"
        method="post"
        class="w-screen h-fit flex flex-col items-center py-10 gap-4 bg-green-200"
      >
        <div
          class="w-1/2 h-10 flex items-center rounded-full px-4 border-black border-[3px] outline-0 duration-200 bg-transparent placeholder:text-gray-700 hover:border-white focus:border-white"
        >
          <%= note.name %>
        </div>
        <input
          type="text"
          name="name"
          placeholder="Enter your new title"
          class="w-1/2 h-10 flex items-center rounded-full px-4 border-black border-[3px] outline-0 duration-200 bg-transparent placeholder:text-gray-700 hover:border-white focus:border-white"
          required
        />
        <pre
          class="w-1/2 h-fit max-h-1/2 overflow-y-scroll flex items-center rounded-xl p-4 border-black border-[3px] outline-0 duration-200 bg-transparent placeholder:text-gray-700 hover:border-white focus:border-white text-slate-600"
          style="scrollbar-width: none"
        >
<%= note.des %></pre
        >
        <textarea
          required
          name="des"
          placeholder="Enter your new disrepsion"
          class="w-1/2 h-30 resize-none rounded-xl px-4 py-1.5 border-black border-[3px] bg-transparent placeholder:text-gray-700 outline-0 hover:border-white focus:border-white"
          style="scrollbar-width: none"
        ></textarea>
        <button class="hover01 w-1/2 h-10 rounded-full">Update Note</button>
      </form>
    </main>
  </body>
</html>
